#include "drivers/HallSwitchDriver.h"
#include <cstdio>
#include <cstring>
#include <unistd.h>
#include <fcntl.h>
#include <errno.h>
#include <sys/ioctl.h>
#include <linux/i2c-dev.h>
#include <linux/i2c.h>

namespace ft {

/* KTH3601 I2C 配置 — 沿用旧板参数，上板后若总线/地址不同再调 */
static constexpr const char* HALL_I2C_BUS = "/dev/i2c-0";
static constexpr uint8_t HALL_DEV_ADDR    = 0x6A;
static constexpr uint8_t HALL_RESET_REG   = 0xF0;
static constexpr uint8_t HALL_EX_MODE_CMD = 0x80;
static constexpr uint8_t HALL_MODE_REG    = 0x18;
static constexpr uint8_t HALL_READ_REG    = 0x48;
static constexpr int     READ_DATA_LEN    = 4;

static const unsigned char CRC_TABLE[256] = {
    
};

unsigned char HallSwitchDriver::crc8(const unsigned char data[])
{
    unsigned char crcData = 0;
    unsigned char frame[8] = {0};
    frame[6] = data[1];
    frame[7] = data[2];
    for (int i = 0; i < 8; i++) {
        crcData = CRC_TABLE[crcData ^ frame[i]];
    }
    return crcData ^ 0x55;
}

bool HallSwitchDriver::init()
{
    std::lock_guard<std::mutex> lock(mutex_);
    if (initialized_) return true;

    int fd = ::open(HALL_I2C_BUS, O_RDWR);
    if (fd < 0) {
        std::fprintf(stderr, "[HallSwitch] open %s failed: %s\n",
                     HALL_I2C_BUS, std::strerror(errno));
        return false;
    }

    struct i2c_msg msg;
    struct i2c_rdwr_ioctl_data msgset;
    msgset.msgs  = &msg;
    msgset.nmsgs = 1;

    auto i2c_xfer = [&](uint8_t reg) -> bool {
        msg.addr  = HALL_DEV_ADDR;
        msg.flags = 0;
        msg.len   = 1;
        msg.buf   = &reg;
        for (int retry = 0; retry < 3; retry++) {
            if (::ioctl(fd, I2C_RDWR, &msgset) >= 0) return true;
            if (retry < 2) ::usleep(100000);
        }
        return false;
    };

    // Reset
    if (!i2c_xfer(HALL_RESET_REG)) {
        std::fprintf(stderr, "[HallSwitch] reset failed\n");
        ::close(fd);
        return false;
    }
    ::usleep(200000);

    // Exit mode
    if (!i2c_xfer(HALL_EX_MODE_CMD)) {
        std::fprintf(stderr, "[HallSwitch] exit mode failed\n");
        ::close(fd);
        return false;
    }
    ::usleep(500000);

    // Continuous mode
    if (!i2c_xfer(HALL_MODE_REG)) {
        std::fprintf(stderr, "[HallSwitch] set continuous mode failed\n");
        ::close(fd);
        return false;
    }
    ::usleep(200000);

    fd_ = fd;
    initialized_ = true;
    return true;
}

void HallSwitchDriver::deinit()
{
    std::lock_guard<std::mutex> lock(mutex_);
    if (!initialized_) return;
    if (fd_ >= 0) {
        ::close(fd_);
        fd_ = -1;
    }
    initialized_ = false;
}

float HallSwitchDriver::readValue()
{
    std::lock_guard<std::mutex> lock(mutex_);
    if (fd_ < 0) return 9999.0f;

    uint8_t reg = HALL_READ_REG;
    uint8_t data[READ_DATA_LEN];
    struct i2c_msg msgs[2];
    struct i2c_rdwr_ioctl_data msgset;

    msgs[0].addr  = HALL_DEV_ADDR;
    msgs[0].flags = 0;
    msgs[0].len   = 1;
    msgs[0].buf   = &reg;

    msgs[1].addr  = HALL_DEV_ADDR;
    msgs[1].flags = I2C_M_RD;
    msgs[1].len   = READ_DATA_LEN;
    msgs[1].buf   = data;

    msgset.msgs  = msgs;
    msgset.nmsgs = 2;

    for (int retry = 0; retry < 3; retry++) {
        if (::ioctl(fd_, I2C_RDWR, &msgset) >= 0) break;
        if (retry < 2) ::usleep(10000);
    }

    unsigned char crc = crc8(data);
    if (crc == data[3]) {
        int16_t raw = static_cast<int16_t>((data[1] << 8) | data[2]);
        return static_cast<float>(raw) / 90.0f;
    }
    return 9999.0f;
}

} // namespace ft
